# Lets any program in the terminal raise a macOS notification via OSC 9 / OSC 777.
desktop-notifications = true

# Visual only: `system` is the macOS alert sound and `audio` plays a file, both off.
bell-features = no-system,no-audio,attention,title,border
